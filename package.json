{
  "name": "profit-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../dist && cp -rv dist/* ../dist",
    "install": "cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "jwks-rsa": "^4.0.1",
    "multer": "^2.1.1",
    "pg": "^8.20.0",
    "resend": "^6.9.4",
    "socket.io": "^4.8.3",
    "uuid": "^9.0.1",
    "web-push": "^3.6.7"
  }
}
