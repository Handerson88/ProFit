{
  "name": "profit-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../public && cp -rv dist/* ../public",
    "install": "cd frontend && npm install"
  }
}
